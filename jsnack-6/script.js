const userNumber = prompt(`Type a number to generate squares`);

function squareNumber (square){
    const newArticle = document.createElement(`article`)
    document.querySelector(`section.output`).appendChild(newArticle)
}